<% if current_user.admin? %>
  <div class="alert alert-info">
    <button type="button" class="close" data-dismiss="alert">×</button>
    You are an Administrator. Please remember to <%= link_to 'Sign out', destroy_user_session_path, :method=>'delete' %> before closing the browser.
  </div>
<% end %>